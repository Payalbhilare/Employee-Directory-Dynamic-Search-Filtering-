# Employee-Directory-Dynamic-Search-Filtering-
The project aims to build a full-stack Employee Directory system using ReactJS (frontend), NodeJS (backend), and PostgreSQL (database). The system will display a dynamic list of employees, allow real-time search and filtering by department, and provide a modal pop-up to display full employee details when an employee entry is selected.
